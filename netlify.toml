[build]
  command = "if [ -n \"$NETLIFY_DATABASE_URL\" ]; then DATABASE_URL=$NETLIFY_DATABASE_URL npm run build:db; fi && DATABASE_URL=${NETLIFY_DATABASE_URL:-postgresql://placeholder:placeholder@placeholder/placeholder} npm run build"
  publish = ".next"
