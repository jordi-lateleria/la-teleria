# For local development with PostgreSQL (Neon):
# Get your connection string from: netlify env:get NETLIFY_DATABASE_URL
# Or use a local PostgreSQL database
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# On Netlify, DATABASE_URL is automatically set from NETLIFY_DATABASE_URL
# via the netlify.toml build command
